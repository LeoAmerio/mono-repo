{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "dev": {
      "cache": false
    },
    "build": {
      "depondsOn": [
        "^build"
      ],
      "outputs": [
        "dist/**"
      ]
    }
  }
}